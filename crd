PS C:\Users\C226796\Downloads> docker buildx build  --tag ${localhost}.jfrog.io/${docker-titan-virtual}/titan:latest .
[+] Building 0.0s (0/0)                                                                                                                                               docker:default
ERROR: invalid tag "//titan:latest": invalid reference format
sudo apt-get install apt-transport-https ca-certificates curl software-properties-common
To uninstall Docker and reinstall it on a Linux system, you can follow these steps:

1. Uninstall Docker:
1.1. Stop the Docker service:

bash
Copy code
sudo systemctl stop docker
1.2. Remove Docker packages:

If you installed Docker using the package manager apt (for Ubuntu, Debian):

bash
Copy code
sudo apt-get purge docker-ce docker-ce-cli containerd.io
If you used yum (for CentOS, RHEL):

bash
Copy code
sudo yum remove docker-ce docker-ce-cli containerd.io
1.3. Remove all Docker data (optional):

If you want to remove all data associated with Docker, including images, containers, volumes, and networks, delete the Docker directory:

bash
Copy code
sudo rm -rf /var/lib/docker
1.4. Remove Docker users and groups (optional):

bash
Copy code
sudo userdel -r docker
sudo groupdel docker
2. Install Docker:
Now that you've uninstalled Docker, you can proceed with a fresh installation.

2.1. Update your package index:

If you are using apt:

bash
Copy code
sudo apt-get update
If you are using yum:

bash
Copy code
sudo yum check-update
2.2. Install dependencies (mainly for apt):

bash
Copy code
sudo apt-get install apt-transport-https ca-certificates curl software-properties-common
2.3. Add Docker's official GPG key:

bash
Copy code
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
2.4. Set up the Docker stable repository:

If you are using apt:

bash
Copy code
sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
If you are using yum, create a Docker repository configuration file:

bash
Copy code
sudo tee /etc/yum.repos.d/docker-ce.repo <<-'EOF'
[docker-ce-stable]
name=Docker CE Stable - $basearch
baseurl=https://download.docker.com/linux/centos/7/$basearch/stable
enabled=1
gpgcheck=1
gpgkey=https://download.docker.com/linux/centos/gpg
EOF
2.5. Install Docker:

If you are using apt:

bash
Copy code
sudo apt-get update
sudo apt-get install docker-ce docker-ce-cli containerd.io
If you are using yum:

bash
Copy code
sudo yum install docker-ce docker-ce-cli containerd.io
2.6. Start Docker and enable it to start on boot:

bash
Copy code
sudo systemctl start docker
sudo systemctl enable docker
2.7. Verify the installation:

bash
Copy code
docker --version
This should display the Docker version you installed.

These steps should help you uninstall and reinstall Docker on your Linux machine. Remember to adjust commands if you are using a different Linux distribution or if you have a different setup.




