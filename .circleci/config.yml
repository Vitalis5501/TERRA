version: 2.1

jobs:
  build:
    docker:
      - image: circleci/python:3.9

    steps:
      - checkout

      - run:
          name: Install dependencies
          command: |
            pip install requests pyral

      - run:
          name: Run the code
          command: |
            python app.py

workflows:
  version: 2
  your_workflow_name:
    jobs:
      - build:
          filters:
            branches:
              only:
                - main
workflows:
  version: 2
  your_workflow_name:
    triggers:
      - schedule:
          cron: "*/2 * * * *"
          filters:
            branches:
              only:
                - master
    jobs:
      - build
